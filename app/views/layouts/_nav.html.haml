%nav.teal{:role => "navigation"}
  .nav-wrapper
    %a.brand-logo{:href => "#!"} Logo
    %a.button-collapse{"data-activates" => "mobile-demo", :href => "#"}
      %i.material-icons menu
    %ul.right.hide-on-med-and-down
      %li
        %a{:href => "about.html"} ABOUT
      - if user_signed_in?
        %li
          = link_to current_user.email, edit_user_registration_path
        %li
          = link_to('LOGOUT', destroy_user_session_path, :method => :delete)
      - elsif doctor_signed_in?
        %li
          = link_to current_doctor.email, edit_doctor_registration_path
        %li
          = link_to('LOGOUT', destroy_doctor_session_path, :method => :delete)
      - else
        %li
          = link_to('PATIENT', new_user_session_path)
        %li
          = link_to('DOCTOR', new_doctor_session_path)

    %ul#mobile-demo.side-nav
      %li
        %a{:href => "about.html"} ABOUT
      - if user_signed_in?
        %li
          = link_to current_user.email, edit_user_registration_path
        %li
          = link_to('LOGOUT', destroy_user_session_path, :method => :delete)
        %li
          = link_to('LOGOUT', destroy_doctor_session_path, :method => :delete)
      -else
        %li
          = link_to('PATIENT', new_user_session_path)
        %li
          = link_to('DOCTOR', new_doctor_session_path)
